# Raw inputs and large reference layers (download per README)
data/raw/
data/reference/
data/spatial/

# data/processed/ IS tracked: the derived analytic datasets are bundled
# so scripts 03+ can be run without re-downloading and re-cleaning raw extracts.

# R session files
.Rhistory
.RData
.Rproj.user/

# OS files
.DS_Store
Thumbs.db

# Output files (regenerated by scripts)
outputs/
